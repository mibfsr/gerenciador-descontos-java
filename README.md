Título e Descrição: O nome do sistema (Gerenciador de Descontos) e uma explicação de duas linhas sobre o problema que ele resolve (aplicação de taxa promocional global sem precisar iterar sobre cada produto no banco de dados).

Conceitos Aplicados: Uma lista com os conceitos de Java utilizados: Encapsulamento, Manipulação de Coleções (List, ArrayList), Expressões Lambda (removeIf) e a diferença prática entre atributos de instância e estáticos (static).

Como Executar: Instruções curtas dizendo que basta rodar a classe Main e interagir com o menu via terminal.